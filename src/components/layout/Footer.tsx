import React from 'react';

// The footer has been entirely integrated into the ContactSection for a more cohesive, immersive ending.
// We export a null component so App.tsx doesn't break.
export const Footer: React.FC = () => {
  return null;
};
