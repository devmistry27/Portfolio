import React from 'react';

export const CustomCursor: React.FC = () => {
  // Disabled per user request to rely strictly on native OS cursors
  // while preserving all other interactive hover states across the application.
  return null;
};
